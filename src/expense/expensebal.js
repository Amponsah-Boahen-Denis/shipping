import React from 'react'

function Expensebal({totalincome}) {
    return (
        <div>
         <h1>income balance</h1>
         <button>{totalincome}</button>   
        </div>
    )
}

export default Expensebal;
